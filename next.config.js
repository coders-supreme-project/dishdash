/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['example.com', 'images.unsplash.com'], // add any other external domains as needed
  },
};

module.exports = nextConfig; 